# ---- Supabase (client-safe; publishable key, protected by RLS) ----
# These already have safe defaults baked into lib/supabase.js, but you can
# override them here or in Vercel's Environment Variables.
NEXT_PUBLIC_SUPABASE_URL=https://deotpmaxtqdafqdhetjm.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_wOk4yp6K4IwovUVLLHjJ9A_DU3_ORYA

# ---- AI answer-checker (SERVER ONLY — never prefix with NEXT_PUBLIC) ----
# Set the key for whichever provider you select on the Settings page.
# 1) Claude (default, cheap & accurate):
ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL=claude-haiku-4-5

# 2) Groq (free tier, fast open models):
# GROQ_API_KEY=
# GROQ_MODEL=llama-3.3-70b-versatile

# 3) OpenRouter (free open models):
# OPENROUTER_API_KEY=
# OPENROUTER_MODEL=meta-llama/llama-3.3-70b-instruct:free
